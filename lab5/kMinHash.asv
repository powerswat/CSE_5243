function kMinHash(tot_mat, tot_vec_lbl)

%% Generate an input matrix (# of shingles * # of docs)
input_mat = tot_mat';

%% Generate parameter values for Approx. Linaer Permutation Hashing
a = randperm(10,1);
b = randperm(10,1)+1;
N = size(input_mat,1);
tmp_p = primes(N+100);
s
h_ab = mod(mod(a.*input_mat(:,1)+ b, p), N);

end